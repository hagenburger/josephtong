%article#london
  %a.previous(href="#") &lt;
  - images_in('london/*.jpg').each do |image|
    %figure
      %img(data-src="#{current_page_relative_path}images/london/#{image}")
  %a.next(href="#") &gt;