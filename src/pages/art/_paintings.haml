%article#paintings
  %a.previous(href="#") &lt;
  - images_in('paintings/*.jpg').each do |image|
    %figure
      %img(data-src="#{current_page_relative_path}images/paintings/#{image}")
  %a.next(href="#") &gt;